# Cleanvey core dependencies
Flask>=3.0
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
rapidfuzz>=3.0
PyYAML>=6.0
anthropic>=0.40        # LLM "judge" — set ANTHROPIC_API_KEY to run the full pipeline

# Dev / testing
pytest>=7.4
